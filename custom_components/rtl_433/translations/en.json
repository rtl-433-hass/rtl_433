{
  "device_automation": {
    "trigger_type": {
      "triggered": "{entity_name} triggered",
      "triggered_subtype": "{entity_name} triggered: {subtype}"
    }
  },
  "config": {
    "step": {
      "user": {
        "title": "Connect to an rtl_433 server",
        "description": "Enter the WebSocket connection details for your rtl_433 HTTP server (started with `-F http`). Use plain `ws://` by default, or enable Secure to use `wss://` through a TLS reverse proxy. No authentication is performed by this integration.",
        "data": {
          "host": "Host",
          "port": "Port",
          "path": "WebSocket path",
          "secure": "Secure (use wss://)",
          "manage_settings": "Manage rtl_433 settings from Home Assistant",
          "discovery_enabled": "Discover new devices",
          "initial_frequency": "Initial frequency (MHz)"
        },
        "data_description": {
          "host": "Hostname or IP address of the rtl_433 server.",
          "port": "TCP port of the rtl_433 HTTP server (default 8433).",
          "path": "WebSocket endpoint path on the server (default /ws).",
          "secure": "Connect over wss:// instead of ws:// (for a TLS reverse proxy).",
          "manage_settings": "When on, Home Assistant adopts this server's current SDR settings and re-applies them on every reconnect, and adds controls for frequency, gain, and more. Turn off to leave the server's own configuration untouched.",
          "discovery_enabled": "When on, newly observed devices on this server are added automatically as nested devices. You can change this later in the hub options.",
          "initial_frequency": "Set the receiver's center frequency in MHz at setup. Defaults to 433.92, the band most rtl_433 sensors use. Clear the field to keep the server's current frequency instead. Only applies when \"Manage rtl_433 settings from Home Assistant\" is on."
        }
      },
      "reconfigure": {
        "title": "Reconfigure rtl_433 hub",
        "description": "Update this hub's connection target, or re-point it at a replacement radio. Nested devices and their history are preserved.",
        "data": {
          "radio_id": "New radio ID (unique_id)",
          "host": "Host",
          "port": "Port",
          "path": "WebSocket path",
          "secure": "Secure (use wss://)"
        },
        "data_description": {
          "radio_id": "The replacement radio's stable ID, shown in the add-on log/status as its unique_id. Leave unchanged to only edit the connection below.",
          "host": "Hostname or IP address of the rtl_433 server.",
          "port": "TCP port of the rtl_433 HTTP server (default 8433).",
          "path": "WebSocket endpoint path on the server (default /ws).",
          "secure": "Connect over wss:// instead of ws:// (for a TLS reverse proxy)."
        }
      },
      "hassio_replace": {
        "title": "Replace a radio?",
        "description": "A new radio published by the {addon} add-on at `{host}:{port}` was discovered. If it replaces a radio on an existing hub, choose that hub to keep its devices and history. Otherwise add it as a new radio.",
        "data": {
          "replaces": "This radio replaces"
        },
        "data_description": {
          "replaces": "Pick the existing hub whose radio you swapped, or \"It's a new radio\" to add it separately."
        }
      },
      "hassio_confirm": {
        "title": "Confirm adding rtl_433 radio",
        "description": "You are about to add a rtl_433 radio published by the {addon} add-on at `{host}:{port}`.",
        "data": {
          "manage_settings": "Manage rtl_433 settings from Home Assistant",
          "discovery_enabled": "Discover new devices",
          "initial_frequency": "Initial frequency (MHz)"
        },
        "data_description": {
          "manage_settings": "When on, Home Assistant adopts this server's current SDR settings and re-applies them on every reconnect, and adds controls for frequency, gain, and more. Turn off to leave the server's own configuration untouched.",
          "discovery_enabled": "When on, newly observed devices on this server are added automatically as nested devices. You can change this later in the hub options.",
          "initial_frequency": "Set the receiver's center frequency in MHz at setup. Defaults to 433.92, the band most rtl_433 sensors use. Clear the field to keep the server's current frequency instead. Only applies when \"Manage rtl_433 settings from Home Assistant\" is on."
        }
      }
    },
    "abort": {
      "already_configured": "This rtl_433 server is already configured.",
      "reconfigure_successful": "Connection settings updated.",
      "rebind_successful": "Radio replaced. Your devices and history are preserved.",
      "invalid_discovery_info": "The discovery message was missing required information."
    },
    "error": {
      "cannot_connect": "Failed to connect. Check the host, port, and path, and that the rtl_433 server is reachable.",
      "id_in_use": "That radio ID is already used by another configured hub."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "rtl_433 options",
        "description": "Choose what to configure.",
        "menu_options": {
          "hub": "Hub settings",
          "device": "Device settings",
          "mappings": "Device mappings"
        }
      },
      "hub": {
        "title": "Hub settings",
        "description": "Configure new-device discovery and the default availability timeout for this rtl_433 server. rtl_433 connection settings are in the three-dot overflow menu under \"reconfigure\".",
        "data": {
          "discovery_enabled": "Discover new devices",
          "availability_timeout": "Availability timeout (seconds)",
          "manage_settings": "Manage rtl_433 settings from Home Assistant"
        },
        "data_description": {
          "discovery_enabled": "When on, newly observed devices on this server are added automatically as nested devices.",
          "availability_timeout": "Mark a device unavailable after this many seconds without an event. Use 0 to never mark devices unavailable. Leave this at the default and each device type is handled automatically: devices that send only rare events (door, window, motion, doorbell) are never marked unavailable, while periodic sensors use 600 seconds. Set a specific value to apply it to every device instead.",
          "manage_settings": "When on, Home Assistant adopts this server's current SDR settings and re-applies them on every reconnect, and adds controls for frequency, gain, and more. Turn off to leave the server's own configuration untouched."
        }
      },
      "device": {
        "title": "Device settings",
        "description": "Pick a device and override its availability timeout. Leave the timeout blank to clear the override and use the hub default. For a utility meter, choose a commodity to calibrate its consumption sensor for the Energy dashboard.",
        "data": {
          "device": "Device",
          "timeout_override": "Availability timeout override (seconds)",
          "commodity": "Meter commodity",
          "motion_clear_delay": "Motion clear delay (seconds)"
        },
        "data_description": {
          "device": "The device whose settings you want to change.",
          "timeout_override": "Mark this device unavailable after this many seconds without an event. Use 0 to never mark it unavailable. Leave blank to use the automatic default for this device's type (longer for door/window/motion sensors).",
          "commodity": "What this meter measures. Pick Energy, Gas, or Water to calibrate the consumption sensor (next step) so it works in the Energy dashboard. Pick None to leave it unitless (and clear any existing calibration).",
          "motion_clear_delay": "For a motion sensor, auto-clear it to off this many seconds after the last motion. Leave blank to use the default."
        }
      },
      "mappings": {
        "title": "Device mappings",
        "description": "Add or correct this hub's device-library mappings as YAML, using the same schema as the shipped library. See [how to write mappings]({docs_url}) for the format, examples, and how to scope an override to one device model. {problems}",
        "data": {
          "user_mappings": "Mappings (YAML)"
        }
      },
      "calibration": {
        "title": "Calibrate consumption ({commodity})",
        "description": "Set the base unit one raw consumption count represents and the scale applied to the counter. The sensor reports raw count x scale in this unit; Home Assistant converts to your preferred display unit. Changing this rebuilds the sensor and starts fresh long-term statistics.",
        "data": {
          "unit": "Base unit",
          "scale": "Scale"
        },
        "data_description": {
          "unit": "The unit one consumption count represents (before scale). Home Assistant converts between compatible units for display.",
          "scale": "Multiplier applied to the raw counter (for example 0.1 if each count is a tenth of a unit). Use 1 if each count is exactly one base unit."
        }
      }
    },
    "abort": {
      "no_devices": "No devices have been discovered yet."
    },
    "error": {
      "invalid_mappings": "Invalid mappings: {problems}"
    }
  },
  "selector": {
    "commodity": {
      "options": {
        "none": "None (unitless)",
        "energy": "Energy",
        "gas": "Gas",
        "water": "Water"
      }
    }
  },
  "issues": {
    "motion_moved_to_binary_sensor": {
      "title": "Motion entities moved to binary sensors",
      "description": "rtl_433 motion is now a binary_sensor (occupancy) with an auto-off clear delay. The old `event.*_motion` entity was removed. Update any automations or dashboards that referenced it to use the new `binary_sensor.*_motion` entity."
    },
    "server_unreachable": {
      "title": "rtl_433 server unreachable: {title}",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "rtl_433 server unreachable",
            "description": "Home Assistant cannot reach the configured rtl_433 server. If you replaced the radio, enter the replacement's ID (its unique_id from the add-on log/status) and connection details below to re-point this hub — your devices and history are preserved. If the server is just temporarily down, leave the fields unchanged and submit to retry; the card also clears automatically once the connection is restored.",
            "data": {
              "radio_id": "New radio ID (unique_id)",
              "host": "Host",
              "port": "Port",
              "path": "WebSocket path",
              "secure": "Secure (use wss://)"
            },
            "data_description": {
              "radio_id": "The replacement radio's stable ID. Leave unchanged to keep the current radio and simply retry.",
              "host": "Hostname or IP address of the rtl_433 server.",
              "port": "TCP port of the rtl_433 HTTP server (default 8433).",
              "path": "WebSocket endpoint path on the server (default /ws).",
              "secure": "Connect over wss:// instead of ws:// (for a TLS reverse proxy)."
            }
          }
        }
      }
    },
    "sample_rate_low_for_band": {
      "title": "rtl_433 sample rate may be low for this frequency: {title}",
      "fix_flow": {
        "step": {
          "init": {
            "title": "Sample rate may be too low",
            "description": "The receiver on {title} is tuned to a high-band frequency but is still sampling at the bare default rate. rtl_433 does not widen the sample rate automatically when it is retuned, so devices in the upper ISM bands may decode poorly.\n\n- **Increase the sample rate** sets the **Sample rate** to {suggested} Hz — the 1.024 MS/s rate rtl_433 itself uses by default.\n- **Keep the current rate** leaves the rate unchanged and stops this advisory from appearing again for this hub. Many devices still decode fine at the lower rate, so this is a valid choice.",
            "menu_options": {
              "apply": "Increase the sample rate",
              "ignore": "Keep the current rate (don't warn again)"
            }
          }
        }
      }
    }
  }
}
